import { LeerlingRichtingFilterPipe } from './leerling-richting-filter.pipe';

describe('LeerlingRichtingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LeerlingRichtingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
