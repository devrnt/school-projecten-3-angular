import { LeerlingFilterPipe } from './leerling-filter.pipe';

describe('LeerlingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LeerlingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
