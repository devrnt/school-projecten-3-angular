import { CompetentieModuleFilterPipe } from './competentie-module-filter.pipe';

describe('CompetentieModuleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompetentieModuleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
