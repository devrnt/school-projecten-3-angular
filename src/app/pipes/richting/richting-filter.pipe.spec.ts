import { RichtingFilterPipe } from './richting-filter.pipe';

describe('RichtingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RichtingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
